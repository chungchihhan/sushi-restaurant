name: Docker Image CI/CD

on:
  push:
    branches: [ "prod" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

    
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker -f backend/Dockerfile --tag sushi-backend:$(date +%s)
    
